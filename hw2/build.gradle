apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDir 'src'
        }
    }
}

jar {
    manifest {
        attributes 'Main-Class': 'Main'
    }
}

task run(type: JavaExec) {
    main = "-jar"
    args jar.archivePath
}